# Gemfile
source "https://rubygems.org"

# Use GitHub Pages gem - this includes Jekyll, Minima, and all plugins
gem "github-pages", group: :jekyll_plugins

# Windows specific gems
gem "tzinfo-data", platforms: [:mingw, :x64_mingw, :mswin, :jruby]

# Add logger to fix Ruby 3.4 warning
gem "logger"

# Note: Removed wdm gem as it's not compatible with Ruby 3.4
# You'll need to manually restart the server when files change